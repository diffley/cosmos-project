# Development Environment Overrides
# Only contains settings that differ from .env.defaults

# Enable local development mode (allows editing files locally)
OPENC3_LOCAL_MODE=1

# Enable demo plugin for development
OPENC3_DEMO=1